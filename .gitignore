# Per-device secrets — never commit.
env/devices/*.local.env

# The profile pointer lives in ~/.config/dotfiles/profile (outside the repo),
# but ignore a stray copy if one ever lands here.
/profile

# nvim plugin lockfile — let each machine float to latest
nvim/.config/nvim/lazy-lock.json
