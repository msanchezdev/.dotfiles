# Shared environment — sourced on every device. Non-secret only.
# Concatenated into ~/.config/dotfiles/env.sh by install.sh.
#
# BARE: nothing exported. Add shared, non-secret settings as you rebuild.

# export EDITOR=nvim
# export VISUAL=nvim
# [ -d "$HOME/.bun/bin" ] && export PATH="$HOME/.bun/bin:$PATH"
