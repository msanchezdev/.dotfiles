# Shared environment — sourced on every device. Non-secret only.
# This file is concatenated into ~/.config/dotfiles/env.sh by install.sh.

export EDITOR=nvim
export VISUAL=nvim

# Toolchain paths (guarded so they only add when present).
[ -d "$HOME/.bun/bin" ] && export PATH="$HOME/.bun/bin:$PATH"
