# Device env for profile: haruna-chan (committed, non-secret).
# Machine-specific shareable settings. Secrets go in
# env/devices/haruna-chan.local.env (gitignored).
#
# BARE: nothing exported.

# CUDA toolkit (this box has a GPU).
# export PATH=/usr/local/cuda-13/bin:$PATH
# export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH
