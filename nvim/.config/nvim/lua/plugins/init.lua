-- Add plugins as files in this directory — one spec (or group) per file, e.g.:
--
--   -- lua/plugins/telescope.lua
--   return {
--     'nvim-telescope/telescope.nvim',
--     dependencies = { 'nvim-lua/plenary.nvim' },
--   }
--
-- lazy.nvim imports every module here. See https://lazy.folke.io for the spec.
return {}
